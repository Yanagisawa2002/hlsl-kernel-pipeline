# Fixed 8 Mi Scan IO-granularity results

The one vector-IO candidate produces correct full outputs and materially lowers the paired point estimate relative to scalar IO, but **all three comparisons are inconclusive under the unchanged stability gates**. Keep it opt-in; do not change defaults, replace RTS, or repeat sampling to obtain a passing result. No Radix GPU samples were added.

| Numerator / denominator | GPU full-operation time ratio [95% CI] | Decision |
|---|---:|---|
| internal-scan-single / internal-scan-single-vector-io | 2.498872 [2.266822, 2.754677] | Inconclusive |
| internal-scan-single / gps-reduce-then-scan | 2.956125 [2.772286, 3.152154] | Inconclusive |
| internal-scan-single-vector-io / gps-reduce-then-scan | 1.182984 [1.099620, 1.272667] | Inconclusive |

Values above one favor the denominator. The interval uses five independent process log ratios, Student t df=4, paired across twelve balanced blocks. Every lower CI and process p95 ratio exceeds1.01, but every arm/process CV exceeds5%. Scalar CV35.35–40.79%, vector38.82–40.11%, RTS9.10–9.75%. Maximum scalar/vector drift is51.54%/36.26%. Within-process operations are correlated and are not independent replicates. Individual comparisons have no multiplicity adjustment. Failed gates are not equivalence and are not repaired by favorable means.

Arithmetic means over five processes are scalar0.236382 ms, vector0.094763 ms, RTS0.076480 ms. Algorithm stage means are0.228609/0.086890/0.073813 ms; reset/scratch stage means0.005273/0.005469/0.000494 ms. CPU recording is0.004827/0.005073/0.007126 ms. These descriptive means differ from paired geometric ratios. The observed reduction sits mainly inside the algorithm stage; input semantics, reset and final output are retained. The data do not establish a stable hardware speedup or a pure percentage attributable to memory instructions.

Only `HLSLPERF_VECTOR_WIDTH` changes from1 to4 in a separately named plan. Each lane retains the same sixteen original-order elements; group256, wave32, scale4, 4096 elements/partition, 2048 partitions, 256 persistent workers, per-thread prefix, wave prefix, eight-wave group scan, coherent lookback and reset remain. The HLSL algorithm text is unchanged. Neighboring lanes still start64 bytes apart. This controls IO granularity independently of semantic local-scan work, not the lane mapping itself.

Actual exported DXIL hashes match all executor compilation hashes in correctness, diagnostics and confirmation. On complete blocks the baseline uses sixteen mask1 raw input loads and sixteen mask1 output stores per lane; the candidate uses four mask15 loads and stores. Static module counts include unreachable-on-full-block tail paths: baseline19 load sites/18 store sites; vector19 load sites/22 store sites, of which4 loads and4 stores are mask15. Do not treat whole-module counts as dynamic execution counts. Both SinglePass entries contain one wave-prefix and one wave-active call, ten barrier sites, three atomic-binop sites and one atomic-CAS site; reset DXIL is identical. The five shared globals are identical: one256-uint array plus four uints,1040 declared bytes. This is compiler IR, not R9700 ISA or measured physical LDS allocation.

Both main-array algorithms still logically read32 MiB and write32 MiB per operation; scratch is24584 bytes. Extra logical bytes and declared shared storage are zero. The vector source uses temporary uint4 values; physical register count, allocation/occupancy, cache traffic, clock state and DRAM transactions were not measured. Chunk/tail lowering and instruction scheduling can change, and faster/slower IO can alter runtime lookback retries despite identical atomic sites. The experiment isolates the selected source-level IO implementation, not a pure hardware latency component. RTS remains at its official defaults (three uint4/thread,3072 elements/partition,2731 partitions, three passes), so differences between candidate and RTS still include their algorithms and lane layouts.

Three retained development processes used distinct seeds, twelve18-operation full batches/arm, six balanced permutation/reverse blocks and eight6-operation warmup batches. Candidate means0.072735/0.074046/0.092589 ms versus scalar0.173018/0.176133/0.217519 ms motivated one confirmation after mechanism/correctness validation. High development variability was retained and is not pooled with confirmation. There were no pass-level probes or software counters in either timing set, only the existing92 timestamp boundaries per full batch.

Correctness:68 candidate boundary/pattern cases (17 sizes including8 Mi; uniform/zeros/ones/extremes) plus the three arms at8 Mi,142 complete A5/5A poisoned-output checks. Formal: five complete processes, fifteen arm/processes,60 complete before/after output checks,360 measured batches/6480 operations,720 warmup operations; all outputs passed and device status remained S_OK. Release builds pass108 Core tests and3 Python protocol tests with0 warnings/errors. The audit checks declaration/order/seeds, real PIDs, runtime files, upstream files, DXIL, output hashes and source checkout/blob bytes.

Failures are preserved. First build: two new tests compared nested shader arrays by identity; fixed structural assertions, plus a readonly-buffer warning fix, without changing the candidate. Declaration generation then rejected scan.hlsl index/checkout byte mismatch; explicit staging under -text/-eol preserves measured checkout bytes. The first formal launch appended an extra diagnostic argument, exited with usage code1 before any GPU process record, and stopped. The launcher condition and argument-count guard were fixed; v2 retains all arms, seeds, order and thresholds and changes only its launcher source hash. No performance sample was discarded or resampled. Both declarations, builds and failed receipts remain.

Measurement source `70a343d5081702797df3d48ecab8fdc244963861`; v2 declaration SHA256 `4bd73c8432af17da19b95e3c05df05ce7b45b7aa7c0149d466d4f50b12064a5d`. Diagnostic source `558ef66611e47fdcb9d39848c66783daacf779f3`. Baseline `67156695bb0eed034078638321212e6df6f98d0a`. R9700, driver32.0.31041.1004, Windows26200, .NET10, Vortice3.8.3, actual DXC/DXIL1.9.2602.17. Official50-file source lock and external defaults/licensing are unchanged. All builds/tests/GPU work used the shared mutex without system changes.

Reproduction: from a clean checkout, run `python tools/causal_scan_protocol.py verify-bytes docs/integration/causal-scan-declaration-v2.json`, then the shared-lock build helper. Run `causal-diagnostic` index0 for correctness/DXIL, indices1..3 for the fixed development schedule, or `causal-formal` against the frozen v2 declaration and binary lock for indices1..5. Use new output directories. See `CAUSAL_SCAN_PLAN.md` and the delivered reproduction script for complete commands. `causal_scan_protocol.py analyze` computes formal statistics; `causal_scan_audit.py` checks the collecting-machine evidence, including recorded absolute paths. Offline archives have per-file SHA256 manifests for relocated verification.
