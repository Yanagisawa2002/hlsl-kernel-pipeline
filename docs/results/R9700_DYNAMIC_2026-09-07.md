# R9700 dynamic-plan correctness evidence — 2026-09-07

**Passed:** 11 positive ABI v2 cases, legacy v1 execution and two deliberately
incorrect shaders rejected. Release build passed with zero warnings/errors;
53 core tests passed, zero failures/skips. This is correctness evidence, not a
performance comparison or a deployment decision.

Tested implementation: `9abf4d31fe5f2a27af6382f903100861f26f7835`.
Device: AMD Radeon AI PRO R9700, driver `32.0.31041.1004`, D3D12, SM 6.0,
Vortice.Dxc `3.8.3.0`, .NET SDK `10.0.302`. The deployed backend, workload,
DXC native assets and dependency manifest have exact hashes in the identity file.

| Gate | Observed result |
|---|---|
| Zero input, zero active, zero consumer capacity | All full-buffer oracles passed; zero indirect X dispatch |
| One item and 65-item partial group | Compacted values, consumed values, count, arguments and bins all passed |
| 4,096 items with seeds 19 / 71 / 103 | Three actual input datasets and independent output oracles passed |
| 4,096 active items, consumer cap 127 | Only bounded prefix consumed; full compacted output also passed |
| `uint.MaxValue` raw count, capacity 129 | GPU-generated arguments and bounded consumer output passed |
| 65,535 items, one thread/group | Actual ExecuteIndirect at the 65,535 group ceiling passed |
| Count and argument offsets of 16 bytes | Whole-buffer hashes passed, including zeroed padding |
| Omitted zero-active output reset | Both poison attempts failed the consumed-output hash, as required |
| Only secondary bin output corrupted | Both bin hashes failed while primary output stayed correct |
| Existing v1 uint-mix workload | Both poison attempts passed with the fixed-dispatch root layout |

Each v2 positive plan has five declared outputs and two poison attempts: 110/110
positive v2 checks passed. Legacy v1 adds 2/2 checks. The two negative plans retain
all 20 checks, including the four expected failures, rather than removing them
from the evidence. The diagnostic timer enclosed the complete repeated plan,
including count/reset, argument generation/clearing, resource transitions and
indirect consumption. No GPU counters, thermal control or clock control were
captured. The shared GPU/Unity measurement lock serialized the native run.

Raw evidence is byte-preserved by `.gitattributes`:

* [Full plans, input bytes, hashes, DXIL identities and diagnostic samples](data/dynamic-r9700-2026-09-07/dynamic-native-smoke.json)
* [Binary identities and artifact checksums](data/dynamic-r9700-2026-09-07/identity.json)
* [Actual native stdout](data/dynamic-r9700-2026-09-07/dynamic-smoke.log)
* [Omitted reset fixture](data/dynamic-r9700-2026-09-07/broken-reset.hlsl)
* [Corrupted secondary output fixture](data/dynamic-r9700-2026-09-07/broken-bins.hlsl)

Reproduce using `tools/Invoke-DynamicValidation.ps1 -SerializationScript
<control>/Invoke-SerializedValidation.ps1`. The matrix is bounded to 14 native
plan validations (11 positive v2, two negatives and one v1). Allow up to a minute
as an operational budget for local build/test/native smoke, excluding shared-lock
wait and first restore; that budget is an estimate, not a measured latency bound.

Remaining integration gates: rerun with the merged measurement/scenario/radix
implementation and acquire the project's declared randomized paired calibration
and independent confirmation evidence. This worker does not claim those gates
passed. The serial compaction demo is not a tuned replacement for the existing
optimized compaction workloads. The Unity consumer intentionally remains v1-only.
